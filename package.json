{
  "name": "ethereum-subgraph",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ GITHUB_USERNAME/SUBGRAPH_NAME",
    "create-local": "graph create --node http://localhost:8020/ GITHUB_USERNAME/SUBGRAPH_NAME",
    "remove-local": "graph remove --node http://localhost:8020/ GITHUB_USERNAME/SUBGRAPH_NAME",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 GITHUB_USERNAME/SUBGRAPH_NAME"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.86.0"
  }
}
